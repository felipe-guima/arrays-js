// map - reescrevendo arrays

const nomes = ["ana JUlia", "CAIo VInicius", "BIA silva"]

const nomesPadrao = nomes.map((nome) => nome.toUpperCase()); // retornar um array declaração em uma linha

console.log(nomesPadrao);